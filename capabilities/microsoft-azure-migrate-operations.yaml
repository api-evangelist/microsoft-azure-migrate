naftiko: 1.0.0-alpha2
info:
  label: Azure Migrate REST API — Operations
  description: 'Azure Migrate REST API — Operations. 1 operations. Lead operation: Azure Migrate REST API List operations.
    Self-contained Naftiko capability covering one Microsoft Azure Migrate business surface.'
  tags:
  - Microsoft Azure Migrate
  - Operations
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    MICROSOFT_AZURE_MIGRATE_API_KEY: MICROSOFT_AZURE_MIGRATE_API_KEY
capability:
  consumes:
  - type: http
    namespace: microsoft-azure-migrate-operations
    baseUri: https://management.azure.com
    description: Azure Migrate REST API — Operations business capability. Self-contained, no shared references.
    resources:
    - name: providers-Microsoft.Migrate-operations
      path: /providers/Microsoft.Migrate/operations
      operations:
      - name: listoperations
        method: GET
        description: Azure Migrate REST API List operations
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    authentication:
      type: bearer
      token: '{{env.MICROSOFT_AZURE_MIGRATE_API_KEY}}'
  exposes:
  - type: rest
    namespace: microsoft-azure-migrate-operations-rest
    port: 8080
    description: REST adapter for Azure Migrate REST API — Operations. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/providers/microsoft-migrate/operations
      name: providers-microsoft-migrate-operations
      description: REST surface for providers-Microsoft.Migrate-operations.
      operations:
      - method: GET
        name: listoperations
        description: Azure Migrate REST API List operations
        call: microsoft-azure-migrate-operations.listoperations
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: microsoft-azure-migrate-operations-mcp
    port: 9090
    transport: http
    description: MCP adapter for Azure Migrate REST API — Operations. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: azure-migrate-rest-api-list
      description: Azure Migrate REST API List operations
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: microsoft-azure-migrate-operations.listoperations
      outputParameters:
      - type: object
        mapping: $.
